language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - cd frontend
  - npm install
  - npm test
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
   path: "frontend/build"
  on:
    branch: master