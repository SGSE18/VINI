import React from 'react';
import { InfoTable } from '../../components/'

const Home = (props) => {
    return (
        <div className="Home-Page">
            <InfoTable />
        </div>
    )
}

export default Home;